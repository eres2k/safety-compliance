# AI Provider Configuration
# Set these in Netlify Environment Variables (Site Settings > Environment Variables)
# Do NOT commit actual API keys to version control

# Claude (Anthropic) API Key - Recommended for better quality
# Get your key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Gemini API Key - Alternative/fallback provider
# Get your key at: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Force a specific AI provider (default: auto)
# Values: auto, claude, gemini
# - auto: Uses Claude if ANTHROPIC_API_KEY is set, otherwise Gemini
# - claude: Force use Claude (requires ANTHROPIC_API_KEY)
# - gemini: Force use Gemini (requires GEMINI_API_KEY)
AI_PROVIDER=auto

# Legacy key (still supported for backwards compatibility)
# VITE_GEMINI_API_KEY=your_gemini_api_key_here
